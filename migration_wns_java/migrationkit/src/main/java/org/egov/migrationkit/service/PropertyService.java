package org.egov.migrationkit.service;

import org.springframework.stereotype.Service;

import io.swagger.client.model.WaterConnection;
@Service
public class PropertyService {

	public String findProperty(WaterConnection conn)
	{
		
		String uuid="0af72ed5-0df3-41de-8749-7275fbfacfe2";
		//write an api call search pt
				//if property found compare with owner name,father name etc.
				//if found return property id
				// if not found create a property from response return the property id
				
		return uuid;
	}

}
